import 'package:flutter/material.dart';

class LastTimeRemove extends StatelessWidget {
  const LastTimeRemove({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}